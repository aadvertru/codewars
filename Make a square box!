function box(n){
  let result = []
  for(let i = 0; i < n; i++) {
    if(i === 0 || i === n-1) {
      result.push('-'.repeat(n))
    } else {
      result.push('-' + ' '.repeat(n - 2) + '-')
    }
  }
  return result
}
